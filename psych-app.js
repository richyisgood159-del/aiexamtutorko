const app=document.getElementById('app');
const papers=window.PSYCH_DATA||[];
const KEY='examTutorPsychV11';
let state={view:'papers',paperId:null,qi:0,answers:{},results:{}};
try{state={...state,...JSON.parse(sessionStorage.getItem(KEY)||'{}')}}catch(e){}
const save=()=>{try{sessionStorage.setItem(KEY,JSON.stringify(state))}catch(e){}};
const paper=()=>papers.find(p=>p.id===state.paperId)||null;
const q=()=>paper()?.questions?.[state.qi]||null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function apiKey(){return sessionStorage.getItem('examTutorOpenRouterKey')||''}
function setKey(){const v=(document.getElementById('apiKey')?.value||'').trim();if(v){sessionStorage.setItem('examTutorOpenRouterKey',v);render()}}
function openPaper(id){state.paperId=id;state.qi=0;state.view='list';save();render()}
function openQ(i){state.qi=i;state.view='viewer';save();render()}
function home(){location.href='index.html'}
function back(){state.view=state.view==='viewer'?'list':'papers';save();render()}
function resetPaper(){const pre=state.paperId+'-';for(const o of [state.answers,state.results])for(const k of Object.keys(o))if(k.startsWith(pre))delete o[k];save();render()}
function key(){return state.paperId+'-'+state.qi}
function answer(){return document.getElementById('answer')?.value?.trim()||''}
function saveAnswer(){const a=document.getElementById('answer')?.value??'';state.answers[key()]=a;save()}
function paperView(){
 app.innerHTML=`<button class="back" onclick="home()">← Biology / home</button><div class="hero"><div><span class="eyebrow">PSYCHOLOGY · v11 PSYCH EXAMINER</span><h1>WPS01 · Social & Cognitive Psychology</h1><p>Six complete exam-series stacks with Pearson-specific AO and levels-based marking.</p></div><div class="stat"><b>${papers.length}</b><span>paper stacks</span></div></div><div class="psych-note"><b>Psychology marking:</b> short answers are marked against the exact paper-specific scheme. 8- and 12-mark responses are judged holistically against that question's Pearson level descriptors, AO balance, command word, application and conclusion/judgement requirements.</div><div class="paperGrid">${papers.map(p=>`<article class="paperCard" onclick="openPaper('${p.id}')"><div class="paperTop"><span class="subjectBadge">PSYCHOLOGY</span><span class="ready">● Ready</span></div><h3>${p.title}</h3><p>${p.code}</p><div class="paperMeta"><span>${p.time}</span><span>${p.marks} marks</span></div><button class="primary">Open stack →</button></article>`).join('')}</div>`;
}
function listView(){const p=paper();app.innerHTML=`<button class="back" onclick="back()">← Psychology papers</button><div class="paperHeader"><div><span class="subjectBadge">PSYCHOLOGY</span><h1>${p.title}</h1><p>${p.code} · ${p.time} · ${p.marks} marks</p></div><div class="pdfBtns"><a href="${p.qp}" target="_blank">Question paper ↗</a><a href="${p.ms}" target="_blank">Mark scheme ↗</a></div></div><div class="paper-tools"><span>${Object.keys(state.results).filter(k=>k.startsWith(p.id+'-')).length} / ${p.questions.length} markable parts attempted</span><button class="secondary" onclick="resetPaper()">Reset paper</button></div><div class="questionList">${p.questions.map((x,i)=>{const r=state.results[p.id+'-'+i];return `<div class="qrow" onclick="openQ(${i})"><div class="qnum">${x.n}</div><div class="grow"><b>Question ${x.n}${x.extended?' · Levels-based':''}</b><span>${x.marks} mark${x.marks!==1?'s':''}${x.drawing?' · manual graph/drawing mark':''}</span></div>${r?`<div class="miniScore">${r.score}/${x.marks}</div>`:'<div class="unattempted">Not attempted</div>'}<div class="chev">›</div></div>`}).join('')}</div>`}
function sourcePath(p,x,kind){return `psych-pages/${p.id}-${kind}-p${kind==='qp'?x.qpPage:x.msPage}.png`}
function sourceImage(p,x,kind,alt){const name=`${p.id}-${kind}-p${kind==='qp'?x.qpPage:x.msPage}.png`;const src=(window.PSYCH_IMAGES||{})[name]||sourcePath(p,x,kind);return `<figure class="question-crop"><img src="${src}" alt="${alt}" loading="eager" decoding="async" onerror="sourceFallback(this,'${kind}','${kind==='qp'?x.qpPage:x.msPage}')"></figure>`}
function sourceFallback(img,kind,page){const p=paper();const pdf=kind==='qp'?p.qp:p.ms;img.closest('figure').outerHTML=`<div class="image-error"><b>${kind==='qp'?'Question':'Mark scheme'} preview could not load.</b><br><a class="open-page" href="${pdf}#page=${page}" target="_blank">Open the exact Pearson page ↗</a></div>`}
function viewer(){const p=paper(),x=q(),k=key(),r=state.results[k],a=state.answers[k]||'';app.innerHTML=`<div class="viewer-nav"><button class="back" onclick="back()">← Questions</button><div><button class="secondary" ${state.qi===0?'disabled':''} onclick="state.qi--;save();render()">← Previous</button> <button class="secondary" ${state.qi===p.questions.length-1?'disabled':''} onclick="state.qi++;save();render()">Next →</button></div></div><div class="viewer"><section class="exam"><div class="examBar"><span>${p.code}</span><b>${x.marks} MARK${x.marks!==1?'S':''}</b></div><div class="qn">Question ${x.n}</div>${x.extended?`<div class="guidance"><b>Levels-based ${x.marks}-marker</b><br>Pearson AO split and level descriptors are used holistically — not keyword counting.</div>`:''}<div class="source-title"><b>Actual exam question</b><span>Shown directly from your Pearson paper so tables, scenarios and figures stay intact.</span></div><div class="source-pages">${sourceImage(p,x,'qp','Original Pearson question')}</div><div class="typed-question always-visible"><div class="typed-label">QUESTION TEXT</div><div class="questionText">${esc(x.questionContext||'Question text unavailable — use the Pearson page above.')}</div></div><textarea id="answer" class="answer" placeholder="Write your exam answer here..." oninput="saveAnswer()">${esc(a)}</textarea><div class="actions">${x.drawing?`<button class="primary" onclick="toggleScheme()">Show mark scheme & self-mark</button>`:`<button class="primary" id="markBtn" onclick="markAI()">✦ Mark written answer</button>`}<button class="secondary" onclick="toggleScheme()">Show Pearson scheme</button><button class="secondary" onclick="clearAnswer()">Clear</button></div>${x.drawing?manualBox(x):''}<div id="schemeBox" class="official-ms" hidden><h3>Actual Pearson mark scheme</h3><p>Original mark-scheme page for this question.</p><div class="source-pages">${sourceImage(p,x,'ms','Official Pearson mark scheme')}</div></div>${r?resultBox(r,x):''}</section><aside class="sidebar"><div class="sideCard"><span class="tiny">PAPER</span><b>${p.title}</b><p>${p.code}</p></div><div class="sideCard"><span class="tiny">PROGRESS</span><b>${Object.keys(state.results).filter(z=>z.startsWith(p.id)).length} / ${p.questions.length} parts attempted</b></div><div class="sideCard"><span class="tiny">QUESTION</span><b>${state.qi+1} of ${p.questions.length}</b><p>${x.extended?`${x.marks}-mark Pearson levels-based response.`:'Short-answer Pearson marking.'}</p></div><div class="ai-status"><span class="ai-dot ${apiKey()?'':'off'}"></span>${apiKey()?'AI examiner connected':'AI examiner not connected'}</div>${!apiKey()&&!x.drawing?`<div class="ai-setup"><b>AI examiner setup</b><p class="ai-note">Connect your OpenRouter key for Psychology marking.</p><input id="apiKey" type="password" autocomplete="off" placeholder="OpenRouter API key"><button class="secondary" onclick="setKey()">Connect AI</button></div>`:''}<a class="sideLink" href="${p.qp}#page=${x.qpPage}" target="_blank">Open original question paper ↗</a></aside></div>`}
function clearAnswer(){state.answers[key()]='';delete state.results[key()];save();render()}
function toggleScheme(){const b=document.getElementById('schemeBox');if(!b)return;b.hidden=!b.hidden;if(!b.hidden)b.scrollIntoView({behavior:'smooth',block:'start'})}
function manualBox(x){return `<div class="psych-note"><b>Graph/drawing question:</b> AI marking is disabled. Complete it on paper or in the PDF, reveal the Pearson scheme, then self-mark.</div><div class="actions"><button class="primary" onclick="toggleScheme()">Show Pearson scheme</button></div><div class="manual-score">${Array.from({length:x.marks+1},(_,i)=>`<button class="secondary" onclick="selfMark(${i})">${i}/${x.marks}</button>`).join('')}</div>`}
function selfMark(s){state.results[key()]={score:s,self:true};save();render()}
function resultBox(r,x){const ao=r.ao||{};return `<div class="result"><div class="resultHead"><div><span class="tiny">MARK</span><div class="bigScore">${r.score}<small>/${x.marks}</small></div></div></div>${r.level?`<p><span class="level-badge">Level ${esc(r.level)}</span></p>`:''}${x.extended?`<div class="ao-grid"><div class="ao-card"><b>AO1</b><br>${esc(ao.AO1||'—')}</div><div class="ao-card"><b>AO2</b><br>${esc(ao.AO2||'—')}</div><div class="ao-card"><b>AO3</b><br>${esc(ao.AO3||'—')}</div></div>`:''}${r.awarded?.length?`<h3>What earned credit</h3><ul>${r.awarded.map(v=>`<li>${esc(v)}</li>`).join('')}</ul>`:''}${r.missed?.length?`<h3>What is missing</h3><ul>${r.missed.map(v=>`<li>${esc(v)}</li>`).join('')}</ul>`:''}${x.extended&&(r.chains||r.balance||r.conclusion)?`<div class="extended-checks"><div><b>Reasoning chains</b><span>${esc(r.chains||'—')}</span></div><div><b>Balance / application</b><span>${esc(r.balance||'—')}</span></div><div><b>Conclusion / judgement</b><span>${esc(r.conclusion||'—')}</span></div></div>`:''}${r.why?`<h3>${x.extended?'Why this level/mark':'Examiner reasoning'}</h3><p>${esc(r.why)}</p>`:''}${r.blocker?`<h3>Why it did not reach the next level</h3><p>${esc(r.blocker)}</p>`:''}${r.next?`<h3>Highest-value improvement</h3><p>${esc(r.next)}</p>`:''}${r.improved?`<h3>Full-mark model</h3><p>${esc(r.improved)}</p>`:''}<button class="secondary" onclick="toggleScheme()">Compare with Pearson scheme</button></div>`}
function line(t,n){const m=t.match(new RegExp('(?:^|\\n)'+n+'\\s*:\\s*(.*)','i'));return m?m[1].trim():''}
function split(v){return String(v||'').split(/\s*\|\s*/).map(s=>s.trim()).filter(s=>s&&!/^(none|n\/a|—)$/i.test(s))}
async function markAI(){
 const x=q(),a=answer(); if(!a)return alert('Write an answer first.');
 const token=apiKey(); if(!token)return alert('Connect your OpenRouter key first.'); saveAnswer();
 const b=document.getElementById('markBtn'); if(b){b.disabled=true;b.textContent='Checking against Pearson…'}
 const command=(x.questionContext.match(/\b(Evaluate|Assess|Discuss|Explain|Describe|State|Calculate|Give|To what extent)\b/i)||[])[1]||'Answer';
 const extended=!!x.extended;
 const rules=extended?`THIS IS A PEARSON LEVELS-BASED ${x.marks}-MARK RESPONSE. The exact level descriptors below control the mark. Do not convert individual bullet points into one mark each. First judge AO quality, then choose the best-fit level, then choose a mark within that level. Equal emphasis means one strong AO cannot compensate for a weak required AO. A response cannot enter Level 4 unless the qualities described for Level 4 are sustained across the response. Do not award top level for merely naming strengths/weaknesses. Developed evaluation/assessment requires linked reasoning explaining WHY evidence affects the claim/theory/method. For EVALUATE require developed competing arguments and a supported conclusion when the supplied descriptor requires one. For ASSESS require judgement of significance/value, not a list. For DISCUSS require developed competing arguments and sustained application when AO2 is required; do not invent a conclusion requirement. Scenario AO2 must explicitly connect psychology to details in the scenario. Generic material does not earn scenario application.`:`SHORT-ANSWER MODE. Mark each required element strictly against this exact question-specific Pearson scheme. Respect AO1/AO2/AO3, linked justification, scenario application, caps, calculation working, units/rounding and any 'generic answers score zero' rule. Never invent a marking point.`;
 const prompt=`You are a senior Pearson Edexcel International A Level Psychology WPS01 examiner.\n${rules}\n\nQUESTION ${x.n} — ${x.marks} marks\nCOMMAND WORD: ${command}\nQUESTION/STIMULUS:\n${x.questionContext}\n\nOFFICIAL QUESTION-SPECIFIC PEARSON MARK SCHEME AND LEVEL DESCRIPTORS:\n${x.scheme}\n\nSTUDENT ANSWER:\n${a}\n\nMARKING PROCEDURE:\n1. Read the exact question and identify which AOs are required.\n2. Compare only against the supplied Pearson scheme.\n3. Quote or closely identify the student's actual wording that earns credit; do not infer unstated knowledge.\n4. For an extended response, separately judge AO1/AO2/AO3 quality, reasoning chains, balance/application and conclusion/judgement. Then select the best-fit Pearson level.\n5. Check the level ABOVE your chosen level and state the exact missing quality preventing promotion.\n6. Choose a mark inside the chosen level based on how securely it meets that descriptor.\n7. A polished writing style alone earns no psychology marks.\n\nReturn ONLY these labelled single lines:\nSCORE: integer 0-${x.marks}\nLEVEL: ${extended?'1, 2, 3 or 4':'N/A'}\nAO1: Weak/Partial/Secure/Thorough/N/A - brief reason\nAO2: Weak/Partial/Secure/Thorough/N/A - brief reason\nAO3: Weak/Partial/Secure/Thorough/N/A - brief reason\nCHAINS: Weak/Partial/Secure/N/A - brief reason\nBALANCE: Weak/Partial/Secure/N/A - brief reason\nCONCLUSION: Missing/Weak/Secure/Not required/N/A - brief reason\nAWARDED: student evidence -> Pearson credit; separate items with |\nMISSED: exact missing/developable requirements; separate items with |\nWHY_LEVEL: why this level and mark are best fit\nBLOCKER: exact reason it does not reach the next level, or 'Top level'\nNEXT: one highest-value change\nIMPROVED: concise full-mark answer for this exact question only`;
 const models=extended?['nvidia/nemotron-3-ultra-550b-a55b:free','inclusionai/ling-3.0-flash:free','openrouter/free']:['inclusionai/ling-3.0-flash:free','nvidia/nemotron-3.5-lightning:free','openrouter/free'];
 const endpoint=(window.EXAM_TUTOR_AI||{}).endpoint||'https://openrouter.ai/api/v1/chat/completions'; let last;
 for(let i=0;i<models.length;i++){
  try{
   if(b)b.textContent=i?'Trying backup examiner…':'Checking against Pearson…';
   const c=new AbortController(),timer=setTimeout(()=>c.abort(),extended?(i?26000:32000):(i?17000:15000)); let res;
   try{res=await fetch(endpoint,{method:'POST',signal:c.signal,headers:{Authorization:'Bearer '+token,'Content-Type':'application/json','HTTP-Referer':location.origin,'X-Title':'Private Exam Tutor'},body:JSON.stringify({model:models[i],temperature:0,max_tokens:extended?1050:620,provider:{sort:'latency',allow_fallbacks:true},messages:[{role:'user',content:prompt}]})})}finally{clearTimeout(timer)}
   if(!res.ok)throw new Error('OpenRouter '+res.status); const d=await res.json(); const t=typeof d?.choices?.[0]?.message?.content==='string'?d.choices[0].message.content:'';
   let num=Number((line(t,'SCORE').match(/\d+/)||[])[0]); if(!Number.isFinite(num))throw new Error('Incomplete examiner response');
   let level=extended?(line(t,'LEVEL').match(/[1-4]/)?.[0]||''):'';
   if(extended && !level)throw new Error('No Pearson level returned');
   // Enforce Pearson level boundaries locally so a model cannot return an impossible score/level pair.
   if(extended){const bands=x.marks===12?{1:[1,3],2:[4,6],3:[7,9],4:[10,12]}:{1:[1,2],2:[3,4],3:[5,6],4:[7,8]};const band=bands[level];if(band)num=Math.max(band[0],Math.min(band[1],num));}
   num=Math.max(0,Math.min(x.marks,Math.round(num))); const awarded=split(line(t,'AWARDED')),missed=split(line(t,'MISSED'));
   if(num>0&&!awarded.length)throw new Error('Examiner gave marks without evidence');
   const rr={score:num,level,ao:{AO1:line(t,'AO1'),AO2:line(t,'AO2'),AO3:line(t,'AO3')},chains:line(t,'CHAINS'),balance:line(t,'BALANCE'),conclusion:line(t,'CONCLUSION'),awarded,missed,why:line(t,'WHY_LEVEL'),blocker:line(t,'BLOCKER'),next:line(t,'NEXT'),improved:line(t,'IMPROVED')};
   state.results[key()]=rr;save();render();return;
  }catch(e){last=e}
 }
 if(b){b.disabled=false;b.textContent='✦ Mark written answer'} alert('Marking did not complete. Your answer is still saved, so you can retry.\n\n'+(last?.message||'Examiner unavailable.'));
}
function render(){if(!state.paperId||state.view==='papers')paperView();else if(state.view==='list')listView();else viewer()}
render();
